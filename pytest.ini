[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    -m "not perf and not slow"
    --ignore=tests/test_gui_pyautogui.py
    --ignore=tests/test_integration_app.py
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    perf: performance tests (excluded from Sprint 8 default run)

# Sprint 8: быстрый прогон — `pytest`
# Полный отчёт с coverage — `python tests/generate_test_report.py`
